#include <stdio.h>
#include <stdlib.h>

int main () {
  int num = 2;
  printf("Valor=%d,endereço=%u", num, &num);
  printf("\n");
  // system("pause");
}

/*
%d => número inteiro decimal
%c => caracter (char)
%f => ponto flutuante (float) real
%s => string
%i => mesmo que %d
%u => decimal sem sinal
%l => inteiro longo (long int)
*/
